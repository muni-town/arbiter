[package]
name = "arbiter-core"
version = "0.1.0"
edition = "2024"

[dependencies]
regorus = { version = "0.11.0", default-features = false, features = ["rvm", "arc", "std"] }

serde_json = "1"

anyhow = "1.0.104"

atrium-xrpc = "0.12.4"